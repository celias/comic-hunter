Audit the relevant parts of the codebase for $ARGUMENTS.

Then create a Linear ticket using the Feature Ticket template with:

- **Acceptance criteria:** Specific, testable checkboxes for what "done" looks like.
- **Implementation plan:** Step-by-step in dependency order. Reference specific files and functions.
- **Testing plan:** What to test, what edge cases to cover, what to manually verify.
- **Dependencies:** What blocks this, what this unblocks, what can run in parallel.

Keep the scope tight — if it's too big, suggest splitting into multiple tickets.
